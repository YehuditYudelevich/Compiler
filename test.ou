x, y: int;
{
    x = 5;
    y = 10;

    if (x < y)
        output(x);
    else
        output(y);

    switch (x) {
        case 1: output(x);
        case 2: output(y);
        default: output(0);
    }
}
